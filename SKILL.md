---
name: minimax-tts-format
description: 将含有 Markdown、LaTeX、代码、HTML、表情符号或口语化动作标注的任意文稿，转换为适合 MiniMax 海螺语音（speech-2.8 系列 TTS）直接合成的朗读文本。自动剥离排版语法、把公式转为口语表达式、按段落与强调点插入 <#x#> 停顿标记、把（笑）（叹气）等动作标注映射为 (laughs)(sighs)(breath) 等语气词标签，并在输出前校验标记合法性。用于口播稿、播客文案、有声书、视频配音、课件朗读等一切需要"拿文本直接喂海螺"的场景。
---

# 海螺语音文稿格式化（MiniMax TTS Format）

把"给人看的文稿"变成"给海螺读的文本"。输入可能是 Markdown、LaTeX、代码片段、网页正文、聊天记录，甚至是一堆带括号动作标注的口播稿；输出必须是 MiniMax TTS 能直接吃掉的 `text` 字段内容。

> 官方接口：`POST /v1/t2a_v2`（或 WebSocket `/ws/v1/t2a_v2`）。本 Skill 只负责产出 `text`，不负责调接口。

## 先记住四条铁律

1. **停顿标记 `<#x#>`**：x 是秒，范围 `0.01–99.99`，最多两位小数。必须夹在两个能发音的文本之间，**绝不能连续出现** `<#0.3#><#0.5#>`，也不能放在开头或结尾。
2. **段落切换 = 换行符**。不要用停顿标记代替换行；换行是官方钦定的分段方式。
3. **语气词标签（换气、笑声等）只有 `speech-2.8-hd` / `speech-2.8-turbo` 支持**。模型选 2.6 或更早时，必须把语气词标签全部清掉，否则会念出 "breath" 之类的内容或报错。
4. **标记用得越省越自然**。句号、逗号海螺自己会停顿。人为插入的停顿只在三处用：强调、话题转换、明确要求。默认密度是"轻"。

## 转换流程（按顺序执行）

### 第 1 步：确认模型与风格

先确认目标模型与用途，再决定转换策略：

- `speech-2.8-hd` / `speech-2.8-turbo` → 启用语气词标签（`(breath)` 等 19 种），可以映射动作标注。
- 其他模型 → 语气词标签一律剥离。
- 问一句用途（口播 / 有声书 / 课件 / 播客）和期望节奏（快 / 自然 / 慢），停顿密度据此调整。

### 第 2 步：按区块处理原始格式

按以下顺序处理，**每一步只处理一种语法，避免正则互相打架**：

1. **代码块**（``` 围栏）→ 默认整块剥离并替换为"（代码略）"，除非用户明确要朗读代码。行内 `` `code` `` → 只剥反引号保留内容（代码符号如 `_`、`*` 继续交给第 4 步处理）。
2. **LaTeX 公式** → 见"LaTeX 口语化"一节。行内 `$...$`、块级 `$$...$$`、`\(...\)` 都要处理。
3. **HTML/XML 标签** → 剥离标签保留内部文本；`<br>`、`</p>` 视为换行；`<#x#>` 停顿标记本身保留（那是我们要的）。
4. **Markdown 排版** → 见"Markdown 剥离"一节。
5. **特殊符号与 emoji** → 见"特殊符号"一节。
6. **停顿与节奏** → 见"停顿策略"一节。
7. **动作标注 → 语气词** → 见"语气词映射"一节。
8. **校验输出** → 见"输出校验"一节。

### 第 3 步：输出

输出三样东西：

1. **转换后的朗读文本**（直接可复制进海螺/接口）。
2. **转换报告**：原字符数 → 新字符数、插入停顿数、语气词标签数、剥离内容清单（哪些公式/代码/图片被拿掉了）。
3. **提醒**：若转换后超 3000 字符建议流式；超 10000 字符必须分段（每段 <10000）。

## LaTeX 口语化

数学公式不能念符号，要念意思。常见规则（中文朗读习惯）：

| LaTeX | 口语 |
| --- | --- |
| `\frac{a}{b}` | a 分之 b |
| `x^2` | x 的平方 |
| `x^n` | x 的 n 次方 |
| `x_{i}` | x 下标 i |
| `\sqrt{x}` | 根号 x |
| `\times` / `\cdot` | 乘 |
| `\div` / `\frac` | 除以 |
| `\pm` | 正负 |
| `\approx` | 约等于 |
| `\neq` | 不等于 |
| `\leq` / `\geq` | 小于等于 / 大于等于 |
| `\sum` | 求和 |
| `\int` | 积分 |
| `\lim` | 极限 |
| `\infty` | 无穷 |
| `\pi` → π | 派（或"圆周率"） |
| `\alpha` `\beta` `\gamma` | 阿尔法、贝塔、伽马 |
| `\Delta` | 德尔塔 |
| `\theta` `\lambda` `\mu` `\sigma` `\omega` | 西塔、兰姆达、缪、西格玛、欧米伽 |
| `a = b` | a 等于 b |
| `a > b` | a 大于 b |

- **嵌套公式先展开最内层**：`\frac{x^2+1}{x-1}` → "x 的平方加 1，分之 x 减 1"（即 `(x^2+1)/(x-1)` 的分式读法）。
- **分子分母各是一串时，在分母前加短停顿**：`<#0.2#>分之` 分隔，避免粘连。
- 简单公式直接口语化；**复杂到无法可靠转口语的**（多行矩阵、复杂积分、化学式），剥离并记录"公式省略"，不要硬编。
- 如果用户说"公式不用读"，直接全部剥离并记录。

## Markdown 剥离

| 语法 | 处理 |
| --- | --- |
| `# 标题`（1–6 级） | 剥 `#`，保留标题文字。一级标题前插 `<#0.5#>`，其余不插 |
| `**粗体**` / `*斜体*` / `__下划线__` / `~~删除线~~` | 剥标记，保留文字（斜体不加重读，海螺不认） |
| `[文字](url)` | 只留"文字" |
| `![alt](url)` | 默认剥离；用户要读图时输出"图片：alt" |
| `> 引用` | 剥 `>`；引用前后各加一个换行 |
| `- 项目` / `* 项目` / `1. 项目`（列表） | 剥列表符；有序列表转"第一，… 第二，…"（≤5 项）或保留数字读法；无序列表各项间插 `<#0.2#>` |
| 表格 | 转成逐行朗读："表头第一项，表头第二项。第一行：…，…。第二行：…，…。"行间插 `<#0.3#>` |
| `---` / `***` 分隔线 | 换行 + `<#0.5#>` |
| `[^1]` 脚注、`<sup>` 等 | 剥离 |
| 缩进代码块（4 空格） | 按代码块处理 |

## 特殊符号与 emoji

| 内容 | 处理 |
| --- | --- |
| `&` | 上下文是连接词 → "和"；否则剥离 |
| `%` | 读作"百分之"，如 `50%` → "百分之五十"（也可保留 `50%`，海螺能读；二选一全篇统一） |
| `±` | "正负" |
| `℃` | "摄氏度" |
| `~` | 剥离 |
| emoji | 默认剥离。表情明显的可映射为语气词：😄😊 → `(chuckle)`、😢😭 → `(sighs)`、😂 → `(laughs)`、😮💨 → `(exhale)`（仅 2.8 模型） |
| 全角括号 `（）` | 若括号内是"笑/叹气"等动作词 → 语气词映射；若是指定读音（见下）→ 保留；否则剥离括号保留内容 |
| URL / 邮箱 | 剥离，替换为"链接"二字；用户要求朗读时才保留 |

**行内发音替换**（官方特性，保留并善用）：
- 普通话拼音带声调数字 1–5，半角括号包裹：`(he2)平` 强制读"和平"的 he2。
- IPA：`(lɪv)`。
- 粤语拼音带声调 1–6：`(sung3)`。
- 多音字/生僻词拿不准时，主动给用户提供拼音标注建议。

## 停顿策略

默认"轻"密度，仅三处插入：

| 场景 | 标记 | 说明 |
| --- | --- | --- |
| 段落切换 | 换行符 | 官方分段方式，不加停顿标记 |
| 强调词前（话题转换、关键词、结论） | `<#0.5#>` | 一稿最多 3–5 处 |
| 短语之间（列举、语义分界） | `<#0.2#>` | 克制使用 |
| 用户原文的 `……` 省略号 | `<#0.4#>` | 替换省略号本身 |
| 用户原文的 `——` 破折号（表停顿） | `<#0.3#>` | 破折号表转折时保留文字"但是"等 |

密度档位（用户可指定）：
- `none`：零插入，只做剥离与映射。
- `light`（默认）：上述规则。
- `medium`：句间一律 `<#0.3#>`，段首强调 `<#0.5#>`。
- `strong`：句间 `<#0.3#>`，关键句前后 `<#0.5#>`，逗号变 `<#0.2#>`。适合极慢节奏朗读。

**严禁插入停顿的位置**：人名前后、复合词中间（如"奥美拉唑肠溶胶囊"）、已有逗号/句号的位置（海螺本来就会停）、两个停顿标记连用、文本开头或结尾。

## 语气词映射（仅 2.8 模型）

用户文稿里的动作标注（中文括号、方括号、尖括号都认），映射到官方 19 个标签：

| 原文标注 | 输出标签 |
| --- | --- |
| （笑）/ [笑] / 哈哈哈 | `(laughs)` |
| （轻笑）/（微笑） | `(chuckle)` |
| （叹气）/（唉） | `(sighs)` |
| （换气）/（深呼吸）/（呼） | `(breath)` |
| （吸气）/（吸一口气） | `(inhale)` |
| （呼气）/（吐气） | `(exhale)` |
| （喘气）/（喘息） | `(pant)` |
| （倒吸气）/（倒抽一口气） | `(gasps)` |
| （咳嗽）/（咳） | `(coughs)` |
| （清嗓子）/（咳嗯） | `(clear-throat)` |
| （吸鼻子） | `(sniffs)` |
| （哼）/（嗤笑） | `(snorts)` |
| （打嗝） | `(burps)` |
| （咂嘴） | `(lip-smacking)` |
| （哼唱）/（哼歌） | `(humming)` |
| （嘶声）/（嘶嘶） | `(hissing)` |
| （嗯）/（唔） | `(emm)` |
| （喷嚏）/（阿嚏） | `(sneezes)` |
| （呻吟） | `(groans)` |
| （停顿）/（停 X 秒） | `<#X#>`，无数字时 `<#0.5#>` |

规则：
- **一个自然段最多 1–2 个语气词标签**，堆多了会油腻。
- 标签放在句首或句尾，不打断句子中间（除非是倒吸气这类情绪爆发点）。
- 原文已含合法标签（`(breath)` 等）→ 校验后保留，不再重复映射。
- 2.8 以下模型 → 全部清除，输出前删除。

## 输出校验（必做）

转换完逐项检查，发现问题就修，修不了就写进报告：

1. **停顿标记合法性**：`<#x#>` 中 x ∈ [0.01, 99.99]，最多两位小数。
2. **无连续停顿**：`<#x#><#y#>` 中间没有任何可发音文字 → 合并或删一个。
3. **停顿位置**：不在开头、不在结尾、两侧都有可发音内容。
4. **语气词标签白名单**：只允许 19 个官方标签；`(applause)` 这类非官方标签删掉。
5. **长度**：<10000 字符；>3000 提醒流式。
6. **残留语法**：全文扫描是否还有 `**`、`$`、`\frac`、`[` 链接、HTML 标签等漏网之鱼。
7. **连续换行**：折叠为单个换行（段落间允许至多一个空行）。

## 示例

**输入（Markdown + LaTeX + 动作标注）：**

```markdown
# 什么是熵？

**熵**（entropy）是系统的混乱程度，记作 $S$。公式 $S = k \ln W$ 中，
$k$ 是玻尔兹曼常数。（停顿）简单说，（笑）熵越大，系统越混乱。

- 熵增加，系统趋于无序
- 熵减少，需要外界做功
```

**输出（speech-2.8-hd）：**

```
<#0.5#>什么是熵？
熵是系统的混乱程度，记作 S。公式 S 等于 k 下标 B ln W 中，k 是玻尔兹曼常数。<#0.5#>简单说，(chuckle)熵越大，系统越混乱。
第一，熵增加，系统趋于无序<#0.2#>第二，熵减少，需要外界做功。
```

**报告：** 剥离 2 处公式（均已口语化）、1 处加粗、1 处无序列表；插入停顿 3 处；语气词 1 个。

## 参考文件

- `references/minimax-tts-spec.md` — 官方格式规范速查（停顿、语气词、发音替换、接口字段）。
- `references/conversion-rules.md` — 全部转换规则与决策树（本文件是它的精简执行版）。
- `references/examples.md` — 更多前后对照示例（长文、课件、播客、代码稿）。
- `scripts/convert_to_tts.py` — 零依赖 Python 转换器，`python3 convert_to_tts.py 输入.txt` 即可一键转换并输出报告。适合批量/程序化处理；人工复杂稿（大量公式、诗歌）仍建议按本文件手工转换。
